# treevial
A Command-Line Interface (CLI) tool designed to make complex C++ diagnostics trivial by analyzing the Abstract Syntax Tree (AST), generating optimal, filtered context for AI models, and allowing you to send prompts to AI directly from within the program.
